# SPDX-License-Identifier: AGPL-3.0-only
name: ${COMPOSE_PROJECT_NAME:-cherrydash}

services:
  server:
    build:
      context: ../..
      dockerfile: deploy/docker/server.Dockerfile
    environment:
      CHERRYDASH_BIND_ADDR: 0.0.0.0:8080
      CHERRYDASH_ENVIRONMENT: ${CHERRYDASH_ENVIRONMENT:-development}
      CHERRYDASH_LOG_JSON: "true"
      RUST_LOG: ${RUST_LOG:-cherrydash=info,tower_http=info}
    ports:
      - "${CHERRYDASH_BIND_HOST:-127.0.0.1}:${CHERRYDASH_SERVER_PORT:-8080}:8080"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:8080/healthz"]
      interval: 10s
      timeout: 3s
      retries: 10
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    restart: unless-stopped
    networks: [cherrydash]

  ingest:
    build:
      context: ../..
      dockerfile: deploy/docker/ingest.Dockerfile
    environment:
      CHERRYDASH_INGEST_BIND_ADDR: 0.0.0.0:8081
      CHERRYDASH_INGEST_WAL_PATH: /var/lib/cherrydash/ingest/events.ndjson
      CHERRYDASH_INGEST_SYNC_WRITES: ${CHERRYDASH_INGEST_SYNC_WRITES:-false}
      CHERRYDASH_INGEST_KEYS_JSON: ${CHERRYDASH_INGEST_KEYS_JSON:-[{"tenant_id":"default","token":"development-only-change-me"}]}
      CHERRYDASH_ENVIRONMENT: ${CHERRYDASH_ENVIRONMENT:-development}
      CHERRYDASH_LOG_JSON: "true"
      RUST_LOG: ${RUST_LOG:-cherrydash=info,tower_http=info}
    volumes:
      - ingest-wal:/var/lib/cherrydash/ingest
    ports:
      - "${CHERRYDASH_BIND_HOST:-127.0.0.1}:${CHERRYDASH_INGEST_PORT:-8081}:8081"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:8081/readyz"]
      interval: 10s
      timeout: 3s
      retries: 10
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    restart: unless-stopped
    networks: [cherrydash]

  edge:
    build:
      context: ../..
      dockerfile: deploy/docker/edge.Dockerfile
    environment:
      CHERRYDASH_INGEST_URL: http://ingest:8081
      CHERRYDASH_INGEST_TOKEN: ${CHERRYDASH_INGEST_TOKEN:-development-only-change-me}
      CHERRYDASH_EDGE_ID: compose-edge-01
      CHERRYDASH_EDGE_INTERVAL_SECONDS: 15
      CHERRYDASH_LOG_JSON: "true"
      RUST_LOG: ${RUST_LOG:-cherrydash=info}
    depends_on:
      ingest:
        condition: service_healthy
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    restart: unless-stopped
    networks: [cherrydash]

  web:
    build:
      context: ../..
      dockerfile: deploy/docker/web.Dockerfile
    ports:
      - "${CHERRYDASH_BIND_HOST:-127.0.0.1}:${CHERRYDASH_WEB_PORT:-3000}:80"
    depends_on:
      server:
        condition: service_healthy
    security_opt: ["no-new-privileges:true"]
    restart: unless-stopped
    networks: [cherrydash]

  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-cherrydash}
      POSTGRES_USER: ${POSTGRES_USER:-cherrydash}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-cherrydash-dev-only}
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ../sql/postgres/001_control_plane.sql:/docker-entrypoint-initdb.d/001_control_plane.sql:ro
    ports:
      - "${CHERRYDASH_ADMIN_BIND_HOST:-127.0.0.1}:${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 10
    restart: unless-stopped
    networks: [cherrydash]

  clickhouse:
    image: clickhouse/clickhouse-server:25.8-alpine
    environment:
      CLICKHOUSE_DB: ${CLICKHOUSE_DB:-cherrydash}
      CLICKHOUSE_USER: ${CLICKHOUSE_USER:-cherrydash}
      CLICKHOUSE_PASSWORD: ${CLICKHOUSE_PASSWORD:-cherrydash-dev-only}
      CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT: "1"
    volumes:
      - clickhouse-data:/var/lib/clickhouse
      - ../sql/clickhouse/001_telemetry.sql:/docker-entrypoint-initdb.d/001_telemetry.sql:ro
    ports:
      - "${CHERRYDASH_ADMIN_BIND_HOST:-127.0.0.1}:${CLICKHOUSE_HTTP_PORT:-8123}:8123"
      - "${CHERRYDASH_ADMIN_BIND_HOST:-127.0.0.1}:${CLICKHOUSE_NATIVE_PORT:-9000}:9000"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8123/ping | grep -q Ok"]
      interval: 10s
      timeout: 5s
      retries: 15
    restart: unless-stopped
    networks: [cherrydash]

  nats:
    image: nats:2.11-alpine
    command: ["-c", "/etc/nats/nats.conf"]
    environment:
      CHERRYDASH_NATS_USER: ${CHERRYDASH_NATS_USER:-cherrydash}
      CHERRYDASH_NATS_PASSWORD: ${CHERRYDASH_NATS_PASSWORD:-cherrydash-dev-only}
    volumes:
      - nats-data:/data
      - ../nats/nats.conf:/etc/nats/nats.conf:ro
    ports:
      - "${CHERRYDASH_ADMIN_BIND_HOST:-127.0.0.1}:${NATS_CLIENT_PORT:-4222}:4222"
      - "${CHERRYDASH_ADMIN_BIND_HOST:-127.0.0.1}:${NATS_MONITOR_PORT:-8222}:8222"
    restart: unless-stopped
    networks: [cherrydash]

  valkey:
    image: valkey/valkey:8-alpine
    command: ["valkey-server", "--appendonly", "yes"]
    volumes:
      - valkey-data:/data
    ports:
      - "${CHERRYDASH_ADMIN_BIND_HOST:-127.0.0.1}:${VALKEY_PORT:-6379}:6379"
    healthcheck:
      test: ["CMD", "valkey-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 10
    restart: unless-stopped
    networks: [cherrydash]

  minio:
    image: quay.io/minio/minio:latest
    command: ["server", "/data", "--console-address", ":9001"]
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-cherrydash}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-cherrydash-dev-only}
    volumes:
      - minio-data:/data
    ports:
      - "${CHERRYDASH_ADMIN_BIND_HOST:-127.0.0.1}:${MINIO_API_PORT:-9002}:9000"
      - "${CHERRYDASH_ADMIN_BIND_HOST:-127.0.0.1}:${MINIO_CONSOLE_PORT:-9003}:9001"
    restart: unless-stopped
    networks: [cherrydash]

networks:
  cherrydash:
    driver: bridge

volumes:
  ingest-wal:
  postgres-data:
  clickhouse-data:
  nats-data:
  valkey-data:
  minio-data:
